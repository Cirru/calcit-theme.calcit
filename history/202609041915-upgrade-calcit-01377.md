# Upgrade checkpoint for Calcit 0.13.77 / Calcit 0.13.77 升级检查点

UTC: 2026-09-04T19:15:44Z

Issue / 任务: [calcit-lang/calcit#746](https://github.com/calcit-lang/calcit/issues/746)

Agent / 身份: `codex-01a06d2e`

Status / 状态: source-ready, blocked only on an immutable Respo release; checkpoint branch, no PR yet. / 源码已就绪，仅等待不可变 Respo release；保存检查点分支，暂不开 PR。

## Migration / 迁移

- Read the complete Calcit 0.13.77 agent editing guide before Snapshot mutation. / 修改 Snapshot 前已完整阅读 Calcit 0.13.77 agent 编辑指南。
- Used final-compatible Calcit 0.13.51 `edit schema` to replace `calcit-theme.comp.container/slurp`'s legacy runtime-style macro schema with a strict phase-aware contract: one `Expr<String>` input, an `Expr<String>` expansion, and the required `:fs-read` compile-time capability. / 使用最终兼容的 Calcit 0.13.51 `edit schema`，将 `slurp` 的旧 runtime 风格宏 schema 迁移为严格的 phase-aware contract：一个 `Expr<String>` 输入、`Expr<String>` 展开结果及必需的编译期 `:fs-read` capability。
- Used only Calcit 0.13.77 tree/edit commands for source changes: normalized the macro's path input with `str`, made DOM event callbacks and Unit-returning helpers end in `&unit`, safely unwrapped the proven string leaf, replaced the struct-incompatible `update-states` call with `update-state-tree` plus typed `:states` access, and migrated deprecated `tag-match` to `match`. / 所有源码修改仅通过 Calcit 0.13.77 tree/edit 命令完成：规范化宏路径输入、让 DOM callback 与 Unit helper 以 `&unit` 结束、安全解包已证明的字符串 leaf、以 `update-state-tree` 和类型化 `:states` 访问替代不兼容 Struct 的 `update-states` 调用，并将弃用的 `tag-match` 迁移为 `match`。
- Pinned compiler and `@calcit/procs` exactly to 0.13.77, updated direct dependencies to current immutable releases, migrated to Yarn 4.12.0, and retained package version 0.4.3 for a separate maintainer release commit. / 将 compiler 与 `@calcit/procs` 精确固定为 0.13.77，更新直接依赖到当前不可变 release，迁移到 Yarn 4.12.0，并保留包版本 0.4.3，供维护者另行执行 release commit。
- Replaced the custom JSON/Node debt checker with the native per-definition Cirru EDN quality baseline at `config/calcit-quality.cirru`. / 使用 `config/calcit-quality.cirru` 的原生逐定义 Cirru EDN quality baseline，替代自制 JSON/Node debt checker。
- Modernized CI with Node 24, Corepack, immutable setup-calcit SHA, least-privilege permissions, strict Caps, exact toolchain checks, canonical-format idempotence, native quality gate, JS/Vite validation, and push-only deployment. / CI 已升级为 Node 24、Corepack、不可变 setup-calcit SHA、最小权限、strict Caps、精确工具链检查、规范格式幂等性、原生 quality gate、JS/Vite 验证及仅 push 部署。

## Published-release evidence / 发布版证据

- Strict Caps resolves all seven modules without warnings. / Strict Caps 无警告解析全部 7 个模块。
- Yarn 4.12.0 immutable install, exact Calcit/procs 0.13.77 verification, canonical format idempotence, JSON/YAML parsing, and whitespace checks pass. / Yarn immutable 安装、精确 0.13.77 工具链、规范格式幂等性、JSON/YAML 解析及空白检查通过。
- Exact 0.13.77 check-only reaches both project entry functions and reports only one warning from published `Respo/respo.calcit@0.16.90` (`d106b38`): `respo.core/decorate-defcomp` passes a generic updater where the new `update` contract expects a Struct-preserving function. / 精确 0.13.77 check-only 可到达两个项目 entry 函数，仅报告发布版 Respo 0.16.90 的一条 `decorate-defcomp` 警告。
- Published Lilac 0.5.8 loads successfully in this dependency graph; it is not an additional blocker for this theme checkpoint. / 发布版 Lilac 0.5.8 在本依赖图中可成功加载，不是本 theme 检查点的额外阻塞。

## Source-linked proof / 源码链接证明

Temporarily replacing only the ignored project module link with the clean merged head of [Respo PR #132](https://github.com/Respo/respo.calcit/pull/132), commit `a6f4c6fcdf46a6508cb90f77690ec956cdf6283e`, makes every remaining gate pass: / 仅临时将被忽略的项目模块链接指向已合并 Respo PR #132 的干净 head 后，以下全部 gate 通过：

- exact 0.13.77 check-only for `main!` and `reload!`; / 精确 0.13.77 的 `main!` 与 `reload!` check-only；
- native quality baseline with zero deltas and zero deprecated calls; / 原生 quality baseline 零增量且零弃用调用；
- definition test discovery (0 selected, 0 failed) and README snippet validation; / definition test discovery（0 selected、0 failed）及 README snippet 验证；
- JavaScript generation and Vite 7.3.6 production build under Node 24.4.1. / Node 24.4.1 下的 JavaScript 生成与 Vite 7.3.6 production build。

`caps reset` then restored every project module link to the immutable published store. No dependency source or module-cache content was edited. / 随后使用 `caps reset` 将所有项目模块链接恢复为不可变发布缓存；未编辑依赖源码或模块缓存内容。

## Resume / 恢复条件

The latest immutable Respo tag remains 0.16.90 even though PR #132 is merged. After a post-fix Respo release is published, reclaim #746, update the exact Respo version, rerun the published-only gates, then open and monitor the focused theme PR. / Respo PR #132 虽已合并，但最新不可变 tag 仍为 0.16.90。待修复后的 Respo release 发布后，重新领取 #746，更新精确 Respo 版本，重跑纯发布版 gate，再创建并持续跟踪聚焦的 theme PR。
